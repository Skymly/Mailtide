# 0.10 Deepen Message relocate behind MailtideApp

## 产品句

Message move, Move-to-Trash, and Restore-from-Trash share one Core relocate ritual. MailtideApp stays the host-facing seam.

False on origin/main: `MoveMessageAsync`, `MoveToTrashAsync`, and `RestoreFromTrashAsync` each copy work-gate → resolve Message/Mailbox → IMAP `MoveAsync` → local MailboxId.

## Non-goals

- New public Core module that only forwards MailtideApp verbs
- Re-extracting Message IMAP flags / Draft origin / Sync / Search (#127)
- Relitigating ADR-0001 (Core + thin hosts), ADR-0002 (EF Core + SQLite), ADR-0003 (Credentials only via Host adapters)
- Changing Person-visible Move / Trash / Restore behaviour
- EmptyTrash (expunge, not relocate)
- Nested Mailbox paths
- Bulk multi-select of arbitrary Messages
- Moving a Unified Inbox thread that spans Accounts
- Shared test FakeImapClient (Worth exploring, not this minor)
- Thinning MailShellView (Worth exploring, not this minor)
- Git tag / GitHub Release packing
- Avalonia control-tree tests
- Per-minor `ready-for-human` 回归票

## Vertical slice

One implement ticket (hop D) crossing the layers that actually change:

1. Internal Message relocate ritual: work-gate, same-Mailbox no-op, IMAP Move then local MailboxId. Move, Move-to-Trash, and Restore-from-Trash call it.
2. Public verbs on `MailtideApp` stay. BrowseShell still takes only `MailtideApp`. `MoveMailboxThreadAsync` keeps calling `MoveMessageAsync`.
3. Existing Core + BrowseShell Move / Trash / Restore tests keep proving Person behaviour. Add coverage that the three verbs share the ritual only if it is observable at the MailtideApp seam (same-Mailbox no-op, IMAP failure leaves the Message).
4. No new public Core module.

## Scope

In:

- Internal Message relocate ritual inside Core
- MoveMessage / MoveToTrash / RestoreFromTrash keep the same host-facing interface

Out:

- New public Core module that only forwards the verbs
- Re-opening #127 partials
- EmptyTrash
- UI dialog changes
- Product work parked below

## Acceptance

- [ ] Move, Move-to-Trash, and Restore-from-Trash still match 0.2 / Trash / Restore behaviour (same-Mailbox no-op, missing destination error, IMAP failure leaves the Message).
- [ ] Relocate ritual lives in one internal place. Deleting it would re-spread the ritual across Move, Move-to-Trash, and Restore.
- [ ] BrowseShell still constructs with `MailtideApp` only. No new public pass-through module.
- [ ] Core + BrowseShell tests green. `.\build.cmd Test` green on the implement PR.

## Issues

| Kind | Issue |
|------|--------|
| Parent | #201 |
| Implement | #203 |

## Worth exploring

- Thin MailShellView click handlers into BrowseShell intents (ADR-0001)
- One shared test FakeImapClient adapter (Core / Desktop / Android copies)
- Nested Mailbox paths
- Bulk multi-select of arbitrary Messages
- Moving a Unified Inbox thread (same-Account Messages only)
