# 0.5 Deepen Mailbox lifecycle behind MailtideApp

## 产品句

Mailbox create and rename share one Core lifecycle ritual. MailtideApp stays the host-facing seam.

False on origin/main: `CreateMailboxAsync` and `RenameMailboxAsync` each re-teach workGate, duplicate Name/Path, BindImapEndpoint, and IMAP-then-local.

## Non-goals

- New public Core module that only forwards MailtideApp verbs
- Re-extracting Message IMAP / Draft origin / Sync / Search (#127)
- Relitigating ADR-0001 (Core + thin hosts), ADR-0002 (EF Core + SQLite), ADR-0003 (Credentials only via Host adapters)
- Changing Create/Rename Person-visible behaviour
- Delete Mailbox
- Nested hierarchy UI or Person-chosen IMAP delimiter
- Conversation / thread grouping in the UI (still Worth exploring)
- Shared test FakeImapClient (Worth exploring, not this minor)
- Thinning MailShellView (Worth exploring, not this minor)
- Git tag / GitHub Release packing
- Avalonia control-tree tests
- Per-minor `ready-for-human` 回归票

## Vertical slice

One implement ticket (hop D) crossing the layers that actually change:

1. Internal Mailbox lifecycle ritual: gate, duplicate Name/Path, IMAP then local row. Create and Rename call it.
2. Public verbs on `MailtideApp` stay. BrowseShell still takes only `MailtideApp`.
3. Existing Core + BrowseShell Create/Rename tests keep proving Person behaviour. Add coverage that both verbs share the ritual only if it is observable at the MailtideApp seam (duplicate, IMAP failure, same-name no-op).
4. No new public Core module.

## Scope

In:

- Internal Mailbox mutate ritual inside Core
- CreateMailbox / RenameMailbox keep the same host-facing interface

Out:

- New public Core module that only forwards the verbs
- Re-opening #127 partials
- UI dialog changes
- Product work parked below

## Acceptance

- [ ] Create and Rename still match 0.3 / 0.4 behaviour (blank, duplicate, IMAP failure, same-name no-op, roles unchanged).
- [ ] Mailbox mutate ritual lives in one internal place. Deleting it would re-spread the ritual across Create and Rename.
- [ ] BrowseShell still constructs with `MailtideApp` only. No new public pass-through module.
- [ ] Core + BrowseShell tests green. `.\build.cmd Test` green on the implement PR.

## Issues

| Kind | Issue |
|------|--------|
| Parent | #171 |
| Implement | hop D — one vertical-slice `ready-for-agent` child of #171 |

## Worth exploring

- Thin MailShellView click handlers into BrowseShell intents (ADR-0001)
- One shared test FakeImapClient adapter (Core / Desktop / Android copies)
- Conversation / thread grouping in the UI (from 0.2 / 0.3 / 0.4)
- Delete Mailbox
- Nested Mailbox paths